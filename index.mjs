/**
 * Add one to the given value.
 * @param {number} value - The value to add one to.
 * @returns {number} The value plus one.
 * @example
 * addOne(1); // 2
 */
export function addOne(value) {
  return value + 1;
}
